liczba = 5;
tekst = "Witaj";

if liczba == length(tekst) and liczba == 5 then
  tekst = concatenate(tekst, " swiecie");
else
  tekst = "Witaj swiecie";

print(tekst);

begin
    if 50 != 50 or not false and true
        liczba = readint;
    else
        tekst = readstr;
end

tekst = substring(tekst, liczba, 4);

print("Pozycja la w Ala ma kota to:");
print(position("Ala ma kota", "la ");
